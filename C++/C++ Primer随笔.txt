c++ primer 第六版 随笔

1 c++中的标准注释方式是 //
 /**/是c语言中的方式，在c99标准中，c中也引入了//注释方式。
建议在c++的文件中，尽量使用//注释方式。

2 c++在预处理阶段会对文件头中的#include<>进行展开。

3 关于using编译指令
	在c++中，为了防止各个库之间命名的函数重名的问题。这里使用命令空间，例如 std::cout
	为了方便，可以使用using编译指令来同一使用命名空间。using namespace std;
	但是这种偷懒的方式会带来问题，尤其在项目中，好的方式是：using std::cout；